import { useEffect, useMemo, useState, type ReactElement, type ReactNode } from "react";
import type { WebuiModelEntry, WebuiSessionListItem, WebuiUsageQuotaResult, WebuiVersionInfo } from "../../server/port.js";
import type { WebuiTransport } from "../contracts.js";

export type SettingsTabKey = "desktop" | "shortcuts" | "voice" | "custom-instructions" | "usage" | "connection" | "account" | "coding" | "worktree" | "archived";
export interface SettingsTabDefinition { readonly key: SettingsTabKey; readonly group: "preferences" | "management" | "coding" | "archived"; readonly label: string; readonly icon: string; }
export const DESKTOP_SETTINGS_TABS: readonly SettingsTabDefinition[] = [
  { key: "desktop", group: "preferences", label: "通用", icon: "desktop" }, { key: "voice", group: "preferences", label: "语音", icon: "voice" }, { key: "shortcuts", group: "preferences", label: "快捷键", icon: "shortcuts" }, { key: "custom-instructions", group: "preferences", label: "个性化", icon: "custom-instructions" },
  { key: "usage", group: "management", label: "用量与模型", icon: "chart" }, { key: "connection", group: "management", label: "连接", icon: "link" }, { key: "account", group: "management", label: "账户", icon: "user" }, { key: "coding", group: "coding", label: "代码审查", icon: "coding" }, { key: "worktree", group: "coding", label: "工作树", icon: "worktree" }, { key: "archived", group: "archived", label: "已归档任务", icon: "archived" },
];
export const SETTINGS_GROUPS = [{ key: "preferences", label: "偏好" }, { key: "management", label: "管理" }, { key: "coding", label: "编码" }, { key: "archived", label: "归档" }] as const;
export function resolveThemePreference(preference: string, systemDark: boolean): "light" | "dark" { return preference === "system" ? (systemDark ? "dark" : "light") : preference === "dark" ? "dark" : "light"; }
export function filterSettingsTabs(query: string): readonly SettingsTabDefinition[] { const needle = query.trim().toLowerCase(); return DESKTOP_SETTINGS_TABS.filter((tab) => !needle || `${tab.label} ${tab.key}`.toLowerCase().includes(needle)); }

const ICONS: Record<string, string> = { desktop: "M14.9996 3.56689H4.99963C3.747 3.56689 2.73303 4.581 2.73303 5.8335V12.938L1.7301 15.189C1.61993 15.408 1.66322 16.3808 2.31799 17.0884H16.983C18.3361 16.3808 18.3787 15.4072 18.2682 15.188L17.2662 12.938V5.8335C17.2662 4.581 16.2522 3.56689 14.9996 3.56689Z", voice: "M11.25 4.25C11.25 3.00736 10.2426 2 9 2C7.75736 2 6.75 3.00736 6.75 4.25V9.5C6.75 10.7426 7.75736 11.75 9 11.75C10.2426 11.75 11.25 10.7426 11.25 9.5V4.25ZM14.25 7.5V9C14.25 11.8995 11.8995 14.25 9 14.25C6.10051 14.25 3.75 11.8995 3.75 9V7.5M9 14.25V16.5M6.75 16.5H11.25", shortcuts: "M13.8768 13.1403H6.10529M2 5.59998C2 4.49541 2.89543 3.59998 4 3.59998H16C17.1046 3.59998 18 4.49541 18 5.59998V14.4C18 15.5045 17.1046 16.4 16 16.4H4C2.89543 16.4 2 15.5045 2 14.4V5.59998Z", "custom-instructions": "M5.24316 2.84784C5.24316 1.57385 6.64787 .800013 7.72461 1.48065L9.60449 3.33514L10.5469 4.78241L11.4404 4.40253C12.9336 3.47824 14.6993 3.09444 16.4414 3.31561V14.6672C14.7097 14.609 13.1971 14.3609 11.873 15.1232L10.2988 16.0295L9.25098 15.7307C7.60003 14.6279 5.89805 14.1654 4.07324 14.3644V4.47479Z", chart: "M2.86194 2.26236V15.5514C2.86194 16.0953 3.30396 16.5368 3.84788 16.5368H17.1373M6.82776 9.53873V13.9655M10.7926 4.44888V13.9655M14.7584 7.62076V13.9655", link: "M10.0002 1.97192A8.0283 8.0283 0 1 0 10.0002 18.0286A8.0283 8.0283 0 0 0 10.0002 1.97192ZM3.19849 10.5999H16.801", user: "M6.25 16.4965V15.25C6.25 14.8522 6.40804 14.4706 7.75 13.75H12.25C13.75 14.8522 13.592 14.4706 13.75 15.25V16.4965M17.5 10A7.5 7.5 0 1 1 2.5 10A7.5 7.5 0 0 1 17.5 10Z", coding: "M11.7852 3.57383L8.34082 16.4264M5.14453 5.74961L1.42969 9.46347L5.99219 14.027M14.0078 5.74961L18.5703 9.46347L14.0078 14.027", worktree: "M11.1538 1.82715V5.42676H10.7524V9.33691H13.9868C15.4225 9.33726 16.5864 10.5008 16.5864 11.9365V12.9736", archived: "M16.0669 3.6062H3.13232C2.12081 3.6062 1.30127 4.42602 1.30127 5.43726V6.05347C1.30127 7.06489 2.12081 7.8855 3.13232 7.8855H16.0669V14.7683C16.0669 16.2334 14.657 17.4216 13.1919 17.4216H6.00635C4.54129 17.4216 3.354 16.2334 3.354 14.7683V7.8855", search: "m21 21-4.3-4.3M10.8 18a7.2 7.2 0 1 1 0-14.4 7.2 7.2 0 0 1 0 14.4", close: "M6 6l12 12M18 6 6 18", back: "M9.57617 3.74332C9.81049 3.509 10.1905 3.509 10.4248 3.74332L3.74219 9.57632L9.57617 16.258C10.1906 16.492 10.1906 16.492 10.4248 16.258L5.61426 10.5998H15.834C16.165 10.5998 16.4336 10.3312 16.4336 10.0002C16.4336 9.66901 16.165 9.40092 15.834 9.40054H5.61426Z" };
export const SETTINGS_ICON_PATHS = ICONS;
export const GENERIC_SECTION_TEST_IDS = ["app-mode-section", "application-section", "link-open-destination-section", "file-section", "session-management-section", "agent-control-permission-section", "preference-settings", "about-section"] as const;
export const GENERIC_RADIO_CONTRACT = { position: "absolute right-4 top-[22px]", accentToken: "icon_default_accent" } as const;
export const GENERIC_FILE_ROW_ORDER = ["file-open-in-new-tab-switch", "file-line-wrap-switch"] as const;
function Icon({ name, size = 18 }: { readonly name: string; readonly size?: number }): ReactElement { const voice = name === "voice"; const stroked = voice || name === "shortcuts" || name === "user"; return <svg aria-hidden="true" data-testid={voice ? "asr-mic-icon" : undefined} className="webui-settings-icon" width={size} height={size} viewBox={voice ? "0 0 18 18" : "0 0 20 20"} fill="none"><path d={ICONS[name] ?? ICONS.desktop} fill={stroked ? "none" : "currentColor"} stroke={stroked ? "currentColor" : "none"} strokeWidth={voice ? "1.08" : "1.2"} strokeLinecap="round" strokeLinejoin="round" /></svg>; }
function stored(key: string, fallback: string): string { return typeof localStorage === "undefined" ? fallback : localStorage.getItem(key) ?? fallback; }
function Switch({ checked, onChange, label, disabled = false, testId }: { readonly checked: boolean; readonly onChange?: (value: boolean) => void; readonly label: string; readonly disabled?: boolean; readonly testId?: string }): ReactElement { return <button type="button" data-testid={testId} role="switch" aria-checked={checked} aria-label={label} aria-disabled={disabled || undefined} disabled={disabled} className={`webui-ant-switch${checked ? " is-checked" : ""}`} onClick={() => onChange?.(!checked)}><span /></button>; }
function Select({ value, onChange, disabled = false, wide = false, testId }: { readonly value: string; readonly onChange?: (value: string) => void; readonly disabled?: boolean; readonly wide?: boolean; readonly testId?: string }): ReactElement { return <label className={`webui-ant-select${wide ? " is-wide" : ""}`} data-testid={testId}><select value={value} disabled={disabled} onChange={(event) => onChange?.(event.target.value)}><option>{value}</option></select><svg aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M12 6L8 10L4 6" stroke="currentColor" strokeLinecap="round" strokeLinejoin="round" /></svg></label>; }
function Button({ children, variant = "gray", disabled = false, onClick }: { readonly children: ReactNode; readonly variant?: "gray" | "black"; readonly disabled?: boolean; readonly onClick?: () => void }): ReactElement { return <button type="button" disabled={disabled} onClick={onClick} className={`webui-mavis-button webui-mavis-button-${variant}`}>{children}</button>; }
function SettingRow({ title, description, children, testId, disabled = false }: { readonly title: string; readonly description?: ReactNode; readonly children?: ReactNode; readonly testId?: string; readonly disabled?: boolean }): ReactElement { return <div data-testid={testId} className={`webui-generic-row${disabled ? " is-disabled" : ""}`} aria-disabled={disabled || undefined}><div className="webui-generic-row-copy"><strong>{title}</strong>{description ? <span>{description}</span> : null}</div><div className="webui-generic-row-control">{children}</div></div>; }
function Divider(): ReactElement { return <div className="webui-generic-divider"><span /></div>; }
function Section({ title, testId, children, preference = false }: { readonly title: string; readonly testId?: string; readonly children: ReactNode; readonly preference?: boolean }): ReactElement { return <section data-testid={testId} className={`webui-generic-section${preference ? " is-preference" : ""}`}><h3>{title}</h3><div className="webui-generic-card">{children}</div></section>; }

/** Capability subset the settings modal actually reads: 9 transport
 *  members plus dataDir / version / sessionId. The single source of truth
 *  for capability shape is `WebuiTransport`; this `Pick<…>` keeps the modal's
 *  real dependency visible on the prop type instead of swallowing the full
 *  68-key contract. Optional semantics are preserved: every picked key
 *  remains `?` because the source field is optional. */
export type WebuiSettingsModalCapabilities = Pick<
  WebuiTransport,
  | "listModels"
  | "selectModel"
  | "getUsageQuota"
  | "getAccountStatus"
  | "listUserModelProviders"
  | "listArchivedSessions"
  | "getMiniMaxApiKeyStatus"
  | "signOut"
  | "deleteSession"
>;

interface SettingsModalProps {
  readonly open: boolean;
  readonly onClose: () => void;
  readonly dataDir?: string;
  readonly version?: WebuiVersionInfo;
  readonly sessionId?: string;
  /** Capability source for the modal. Typed as the narrow 9-member
   *  contract so the modal cannot accidentally start reading members it
   *  does not consume. */
  readonly transport?: WebuiSettingsModalCapabilities;
}

export function SettingsModal({ open, onClose, dataDir, version, sessionId, transport }: SettingsModalProps): ReactElement | null {
  // The transport is optional. Each capability is optional too, so we bind
  // only when both are present; otherwise we surface `undefined` and let the
  // call sites do their existing null checks.
  const listModels = transport?.listModels ? transport.listModels.bind(transport) : undefined;
  const selectModel = transport?.selectModel ? transport.selectModel.bind(transport) : undefined;
  const getUsageQuota = transport?.getUsageQuota ? transport.getUsageQuota.bind(transport) : undefined;
  const getAccountStatus = transport?.getAccountStatus ? transport.getAccountStatus.bind(transport) : undefined;
  const listUserModelProviders = transport?.listUserModelProviders ? transport.listUserModelProviders.bind(transport) : undefined;
  const listArchivedSessions = transport?.listArchivedSessions ? transport.listArchivedSessions.bind(transport) : undefined;
  const getMiniMaxApiKeyStatus = transport?.getMiniMaxApiKeyStatus ? transport.getMiniMaxApiKeyStatus.bind(transport) : undefined;
  const signOut = transport?.signOut ? transport.signOut.bind(transport) : undefined;
  const deleteSession = transport?.deleteSession ? transport.deleteSession.bind(transport) : undefined;
  const [active, setActive] = useState<SettingsTabKey>("desktop"); const [query, setQuery] = useState(""); const [theme, setTheme] = useState(() => stored("webui-theme", "light")); const [systemDark, setSystemDark] = useState(() => typeof window !== "undefined" && window.matchMedia?.("(prefers-color-scheme: dark)").matches); const [language, setLanguage] = useState(() => stored("mavis-locale", "en")); const [wrap, setWrap] = useState(() => stored("file_line_wrap", "true") === "true"); const [newTab, setNewTab] = useState(() => stored("file_open_in_new_tab", "false") === "true"); const [followUp, setFollowUp] = useState(() => stored("webui-follow-up-behavior", "queue")); const [contextWindow, setContextWindow] = useState(() => stored("webui-context-window-usage", "false") === "true"); const [models, setModels] = useState<readonly WebuiModelEntry[]>([]); const [quota, setQuota] = useState<WebuiUsageQuotaResult>(); const [account, setAccount] = useState<Record<string, unknown>>(); const [providers, setProviders] = useState<readonly Record<string, unknown>[]>([]); const [apiKeyStatus, setApiKeyStatus] = useState<Record<string, unknown>>(); const [archived, setArchived] = useState<readonly WebuiSessionListItem[]>([]); const [signOutError, setSignOutError] = useState<string>(); const actualTheme = resolveThemePreference(theme, systemDark);
  useEffect(() => { if (typeof window === "undefined" || !window.matchMedia) return; const media = window.matchMedia("(prefers-color-scheme: dark)"); const listener = () => setSystemDark(media.matches); listener(); media.addEventListener?.("change", listener); return () => media.removeEventListener?.("change", listener); }, []);
  useEffect(() => { if (typeof document === "undefined") return; document.documentElement.classList.toggle("dark", actualTheme === "dark"); document.documentElement.classList.toggle("light", actualTheme !== "dark"); document.documentElement.lang = language.startsWith("zh") ? "zh-CN" : "en"; localStorage?.setItem("webui-theme", theme); localStorage?.setItem("mavis-locale", language); localStorage?.setItem("file_line_wrap", String(wrap)); localStorage?.setItem("file_open_in_new_tab", String(newTab)); localStorage?.setItem("webui-follow-up-behavior", followUp); localStorage?.setItem("webui-context-window-usage", String(contextWindow)); }, [actualTheme, contextWindow, followUp, language, newTab, theme, wrap]);
  useEffect(() => { if (!open) return; let cancelled = false; void Promise.all([listModels?.({ sessionId }), getAccountStatus?.({ sessionId }), getUsageQuota?.(), listUserModelProviders?.(), getMiniMaxApiKeyStatus?.(), listArchivedSessions?.()]).then(([nextModels, nextAccount, nextQuota, nextProviders, nextKeyStatus, nextArchived]) => { if (cancelled) return; if (nextModels) setModels(nextModels); if (nextAccount) setAccount(nextAccount); if (nextQuota) setQuota(nextQuota); if (nextProviders) setProviders(nextProviders); if (nextKeyStatus) setApiKeyStatus(nextKeyStatus); if (nextArchived) setArchived(nextArchived.sessions); }); return () => { cancelled = true; }; }, [getAccountStatus, getMiniMaxApiKeyStatus, getUsageQuota, listArchivedSessions, listModels, listUserModelProviders, open, sessionId]);
  const visibleTabs = useMemo(() => filterSettingsTabs(query), [query]); if (!open) return null; const selected = models.find((model) => model.selected); const modelValue = selected ? `${selected.providerId}/${selected.modelId}/${selected.variant ?? ""}` : ""; const groups = SETTINGS_GROUPS.map((group) => ({ ...group, tabs: visibleTabs.filter((tab) => tab.group === group.key) })).filter((group) => group.tabs.length > 0); const label = DESKTOP_SETTINGS_TABS.find((tab) => tab.key === active)?.label; const usage = quota?.signedIn ? quota.quota : undefined;
  const changeModel = async (value: string) => { const model = models.find((candidate) => `${candidate.providerId}/${candidate.modelId}/${candidate.variant ?? ""}` === value); if (!model || !selectModel) return; await selectModel({ providerId: model.providerId, modelId: model.modelId, ...(model.variant ? { variant: model.variant } : {}), ...(sessionId ? { sessionId } : {}) }); }; const handleSignOut = async () => { if (!signOut) return; try { setSignOutError(undefined); await signOut(); onClose(); } catch (error) { setSignOutError(error instanceof Error ? error.message : String(error)); } };
  return <div role="dialog" aria-modal="true" aria-label="设置" className="webui-settings-mask" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><section className="webui-settings-modal" onMouseDown={(event) => event.stopPropagation()}><aside className="webui-settings-sidebar"><button type="button" aria-label="返回" className="webui-settings-back" onClick={onClose}><Icon name="back" /><span>返回应用</span></button><div className="webui-settings-search"><Icon name="search" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="搜索设置..." aria-label="搜索设置" />{query ? <button type="button" aria-label="清空设置搜索" onClick={() => setQuery("")}><Icon name="close" /></button> : null}</div><nav aria-label="设置分类" className="webui-settings-nav">{groups.length ? groups.map((group) => <div key={group.key} className="webui-settings-group"><h3>{group.label}</h3>{group.tabs.map((tab) => <button type="button" key={tab.key} data-menu-key={tab.key} className={`webui-settings-nav-item menu-item${active === tab.key ? " is-active active" : ""}`} aria-current={active === tab.key ? "page" : undefined} onClick={() => setActive(tab.key)}><span className="menu-icon"><Icon name={tab.icon} /></span><span className="menu-label">{tab.label}</span></button>)}</div>) : <p className="webui-settings-no-results">没有匹配的设置</p>}</nav></aside><main className="webui-settings-content" key={active}><header className="webui-settings-content-header"><h2>{label}</h2></header>{active === "desktop" ? <GenericPage theme={theme} setTheme={setTheme} language={language} setLanguage={setLanguage} wrap={wrap} setWrap={setWrap} newTab={newTab} setNewTab={setNewTab} followUp={followUp} setFollowUp={setFollowUp} contextWindow={contextWindow} setContextWindow={setContextWindow} version={version?.version ?? ""} /> : null}{active === "usage" ? <div className="webui-settings-panels"><SettingPanel title="模型"><SettingRow title="当前模型" description="用于新会话的默认模型">{models.length ? <Select value={modelValue} onChange={(value) => void changeModel(value)} /> : null}</SettingRow></SettingPanel>{usage ? <SettingPanel title="用量"><SettingRow title="5 小时窗口" description={`${usage.fiveHour.usedPercent ?? 0}%`} /><SettingRow title="每周窗口" description={`${usage.weekly.usedPercent ?? 0}%`} /></SettingPanel> : null}{providers.length || apiKeyStatus ? <SettingPanel title="Provider"><SettingRow title="已配置 Provider" description={`${providers.length} 个`} /><SettingRow title="MiniMax API key" description={apiKeyStatus?.maskedApiKey ? String(apiKeyStatus.maskedApiKey) : apiKeyStatus?.hasApiKey ? "已配置" : "未配置"} /></SettingPanel> : null}</div> : null}{active === "account" ? <div className="webui-settings-panels"><SettingPanel title="账户"><SettingRow title="账户信息" description={typeof account?.email === "string" ? account.email : ""} /><Button disabled={!signOut} onClick={handleSignOut}>退出登录</Button>{signOutError ? <p role="alert" className="webui-settings-error">{signOutError}</p> : null}</SettingPanel></div> : null}{active === "archived" ? <div className="webui-settings-panels"><SettingPanel title="已归档任务"><div className="webui-settings-archived-list">{archived.length ? archived.map((session) => <div className="webui-settings-row" key={session.sessionId}><div><strong>{session.title || session.sessionId}</strong><span>{session.workspaceDir ?? ""}</span></div><Button onClick={() => void deleteSession?.({ id: session.sessionId })}>删除</Button></div>) : <p className="webui-settings-empty-panel">暂无已归档任务</p>}</div></SettingPanel></div> : null}{active !== "desktop" && active !== "usage" && active !== "account" && active !== "archived" ? <div className="webui-settings-empty-panel" aria-label="空设置面板" /> : null}{active === "desktop" && dataDir ? <p className="webui-settings-data-dir">{dataDir}</p> : null}</main></section></div>;
}

function GenericPage({ theme, setTheme, language, setLanguage, wrap, setWrap, newTab, setNewTab, followUp, setFollowUp, contextWindow, setContextWindow, version }: { readonly theme: string; readonly setTheme: (value: string) => void; readonly language: string; readonly setLanguage: (value: string) => void; readonly wrap: boolean; readonly setWrap: (value: boolean) => void; readonly newTab: boolean; readonly setNewTab: (value: boolean) => void; readonly followUp: string; readonly setFollowUp: (value: string) => void; readonly contextWindow: boolean; readonly setContextWindow: (value: boolean) => void; readonly version: string }): ReactElement {
  const off = (title: string, description: ReactNode, testId?: string) => <SettingRow title={title} description={description} testId={testId} disabled><Switch checked={false} label={title} disabled /></SettingRow>;
  return <div data-testid="content-body" className="webui-generic-page"><Section title="模式" testId="app-mode-section"><div data-testid="app-mode-options" className="webui-mode-options"><ModeCard testId="app-mode-option-coding" title="适用于编程开发" description="保留技术细节与开发工具" icon="coding" selected /><ModeCard testId="app-mode-option-work" title="适用于日常工作" description="同样强大，减少技术细节干扰" icon="desktop" /></div></Section><Section title="应用" testId="application-section"><Appearance theme={theme} setTheme={setTheme} /><Divider />{off("显示在菜单栏", "在菜单栏/系统托盘显示应用图标") }<Divider />{off("开机自启动", "登录时自动启动应用") }<Divider />{off("桌面通知", "任务完成、出错、需要权限审批等阻塞状态时，发送系统通知提醒") }<Divider />{off("加入提前灰度", "优先体验最新版本功能", "early-access-update-switch") }<Divider />{off("加速索引", "开启后，MiniMax Code 会基于工作区生成语义索引，加快代码搜索的速度", "workspace-indexing-switch") }<Divider /><SettingRow title="语言" description="设置应用语言"><Select value={language === "zh-CN" ? "中文" : "English"} onChange={(value) => setLanguage(value === "中文" ? "zh-CN" : "en")} /></SettingRow></Section><Section title="链接" testId="link-open-destination-section"><SettingRow title="网页链接打开位置" description="公开网页链接默认打开位置" testId="web-link-open-destination-row"><Select value="内置浏览器" wide disabled testId="web-link-open-destination-row-select" /></SettingRow><Divider /><SettingRow title="本地链接打开位置" description="本地开发页面默认打开位置" testId="local-link-open-destination-row"><Select value="内置浏览器" wide disabled testId="local-link-open-destination-row-select" /></SettingRow></Section><Section title="文件" testId="file-section"><SettingRow title="在新的标签页打开文件" description="关闭后，默认复用未固定的文件标签页；已固定的标签页会保留。" testId="file-open-in-new-tab-switch"><Switch checked={newTab} onChange={setNewTab} label="在新的标签页打开文件" /></SettingRow><Divider /><SettingRow title="文件预览自动换行" description="开启后，超出预览区域宽度的文本和代码会自动折行；关闭后可横向滚动查看。不修改文件内容。" testId="file-line-wrap-switch"><Switch checked={wrap} onChange={setWrap} label="文件预览自动换行" /></SettingRow></Section><Section title="会话管理" testId="session-management-section"><SettingRow title="显示上下文窗口使用情况" testId="context-window-usage-switch"><Switch checked={contextWindow} onChange={setContextWindow} label="显示上下文窗口使用情况" /></SettingRow></Section><Section title="Agent 控制权限" testId="agent-control-permission-section"><SettingRow title="自动打开浏览器面板" description="Agent 操作网页时，自动打开右侧浏览器面板" testId="browser-use-auto-open-row"><Switch checked={false} label="自动打开浏览器面板" disabled testId="browser-use-auto-open-switch" /></SettingRow></Section><Section title="偏好设置" testId="preference-settings" preference><SettingRow title="跟进消息行为" description="任务运行中按 Enter 发送跟进消息时：排队等待，或立即发送到当前任务。{{shortcut}} 可单次反转。"><div className="webui-segmented" role="radiogroup">{([['queue', '排队'], ['steer', '立即发送']] as const).map(([value, text]) => <button type="button" key={value} role="radio" aria-checked={followUp === value} className={followUp === value ? "is-selected" : ""} onClick={() => setFollowUp(value)}>{text}</button>)}</div></SettingRow><Divider />{off("去除 AI 生成水印", <>关闭时，下载内容将包含显式 AI 生成水印；开启去除水印即代表你已阅读并同意 <a href="#download-watermark-rules">《去水印规则》</a></>) }<Divider />{off("数据用于优化体验", "允许我们将你的对话内容用于优化MiniMax Code的使用体验。我们保障你的数据隐私安全。") }</Section><Section title="关于" testId="about-section"><SettingRow title="上传日志" description="上传应用日志以协助排查问题"><Button disabled>上传</Button></SettingRow><Divider /><SettingRow title="应用版本" description={version}><Button variant="black" disabled>检查更新</Button></SettingRow></Section></div>;
}
function Appearance({ theme, setTheme }: { readonly theme: string; readonly setTheme: (value: string) => void }): ReactElement { return <SettingRow title="外观" description="选择应用的显示主题" testId="mavis-settings-appearance-row"><div className="mavis-settings-theme-selector">{([['light', '浅色模式', 'light.d3fbb1aa.svg'], ['dark', '深色模式', 'dark.14c569ba.svg'], ['system', '跟随系统', 'system.aba90841.svg']] as const).map(([value, text, src]) => <button type="button" key={value} aria-pressed={theme === value} className="mavis-settings-theme-option" onClick={() => setTheme(value)}><span className={`mavis-settings-theme-preview${theme === value ? " is-selected" : ""}`}><span><img src={`/assets/img/${src}`} alt={`${text}预览`} draggable="false" /></span></span><span>{text}</span></button>)}</div></SettingRow>; }
function ModeCard({ testId, title, description, icon, selected = false }: { readonly testId: string; readonly title: string; readonly description: string; readonly icon: string; readonly selected?: boolean }): ReactElement { return <button type="button" data-testid={testId} data-selected={selected} aria-pressed={selected} disabled className="webui-mode-card"><span className="webui-mode-inner"><span className="webui-mode-icon"><Icon name={icon} size={24} /></span><span className="webui-mode-copy"><strong data-testid={`${testId}-title`}>{title}</strong><span data-testid={`${testId}-description`}>{description}</span></span><span data-testid={`${testId}-radio`} className={`webui-mode-radio${selected ? " is-selected" : ""}`}>{selected ? "●" : "○"}</span></span></button>; }
function SettingPanel({ title, children }: { readonly title: string; readonly children: ReactNode }): ReactElement { return <section className="webui-settings-panel"><h3>{title}</h3><div>{children}</div></section>; }
